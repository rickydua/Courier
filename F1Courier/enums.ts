export enum ParcelType {
  SMALL = 'SMALL',
  MEDIUM = 'MEDIUM',
  LARGE = 'LARGE',
  XLARGE = 'XLARGE',
  UNKNOWN = 'UNKNOWN',
}

export enum CostAmount {
  SMALL = 3,
  MEDIUM = 8,
  LARGE = 15,
  XLARGE = 25,
  UNKNOWN = 0,
}

// all units in cm
export enum ParcelDimension {
  SMALL_PARCEL = 10,
  MEDIUM_PARCEL = 50,
  LARGE_PARCEL = 100,
}
